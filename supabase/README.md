# Supabase setup

Run the SQL in `migrations/001_prayer_requests_and_rls.sql` in your Supabase project (**SQL Editor**) before using:

- Prayer & counseling form submissions
- Secured sermon uploads/deletes (authenticated pastors only)

After running the migration, confirm pastor accounts exist under **Authentication → Users**.
